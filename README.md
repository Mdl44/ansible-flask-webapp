# ansible-flask-webapp
A modern web interface for managing Ansible playbooks, hosts, groups, and SLURM jobs, built with Flask, SQLAlchemy, and a responsive UI. Includes user authentication, admin dashboard, and integration with MySQL and SLURM reporting.
